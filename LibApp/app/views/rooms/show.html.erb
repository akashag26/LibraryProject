<%= stylesheet_link_tag 'rooms' %>
<h1>Listing Rooms</h1>

<table align="center">
  <thead>
  <tr>
    <th>Room No</th>
    <th>Size</th>
    <th>Building</th>
    <th>Status</th>
  </tr>
  </thead>

  <tbody>
  <% @rooms.each do |room| %>
      <tr>
        <td><%= room.room_no %></td>
        <td><%= room.size %></td>
        <td><%= room.building %></td>
        <td><%= room.status %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add room', rooms_add_path %>
